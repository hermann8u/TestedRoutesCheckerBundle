on: [pull_request]
name: Tests

jobs:
  tests:
    name: PHPUnit
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        php: [ 8.4, 8.5 ]
        symfony: [ 7.4.*, 8.0.*, 8.1.* ]

    steps:
      - name: Check out repository code
        uses: actions/checkout@v7

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none

      - name: Cache Composer
        uses: actions/cache@v5
        with:
          path: ~/.cache/composer
          key: composer-${{ matrix.php }}-sf${{ matrix.symfony }}-${{ hashFiles('composer.json') }}
          restore-keys: composer-${{ matrix.php }}-sf${{ matrix.symfony }}-

      - name: Activate Symfony Flex
        run: |
          composer global config --no-plugins allow-plugins.symfony/flex true
          composer global require --no-progress --no-scripts --no-plugins symfony/flex

      - name: Install dependencies
        env:
          SYMFONY_REQUIRE: ${{ matrix.symfony }}
        run: composer update --prefer-stable --no-interaction --no-progress --optimize-autoloader

      - name: Run PHPUnit
        run: vendor/bin/phpunit
